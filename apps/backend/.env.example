# verifd Backend Configuration

# Rate Limiting
PASSCHECK_RPM_IP=5                    # Rate limit per IP for /pass/check (requests/minute)
PASSCHECK_RPM_NUMBER=10               # Rate limit per phone number for /pass/check (requests/minute)

# CORS Configuration
WEB_VERIFY_DEV_ORIGIN=http://localhost:3000  # Allowed origin for web-verify CORS

# Privacy & Security
LOG_SALT=your-custom-salt-here        # Salt for phone number hashing in logs (change in production)

# Database (Development)
# Note: Uses SQLite by default. For production, configure PostgreSQL connection.

# Test Configuration (Optional)
# RUN_DB_E2E=1                        # Enable database integration tests
# USE_SQLJS_FOR_TESTS=1               # Alternative database test enabler