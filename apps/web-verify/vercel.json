{
  "buildCommand": "cd ../.. && pnpm -r --filter @verifd/shared --filter web-verify build",
  "installCommand": "pnpm install --frozen-lockfile",
  "rewrites": [
    {
      "source": "/v/(.*)",
      "destination": "/success?code=$1"
    }
  ]
}